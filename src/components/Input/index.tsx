import { Container } from './styles';
import React from 'react';
import type { InputProps } from './types';

export default function Input({ className }: InputProps): JSX.Element {
  return <Container className={className}>asd</Container>;
}
